# Upstream reveal.js — cloned at build time, never committed
.playwright-mcp
/reveal.js/

# Node deps
node_modules/

# Build artifacts — CI rebuilds these, served via GitHub Pages
/dist/
.cache/
.parcel-cache/
.DS_Store

# OS / editor
*.swp
*.tmp
.idea/
.vscode/
